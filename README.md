# Github pages

## Run

* dev: `yarn run start`

>  http://localhost:8080/

* prod: `yarn run build`

> https://pchapl.dev

## Deploy

Script `deploy.sh` will clean up, build, commit and push site to the `master` branch
